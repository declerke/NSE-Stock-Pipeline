name: 'nse_pipeline'
version: '1.0.0'
config-version: 2

profile: 'nse_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nse_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
